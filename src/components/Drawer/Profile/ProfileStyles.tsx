import { Grid } from '@kvika/theme';
import styled from 'styled-components/native';

export const StyledContainer = styled.View`
  display: flex;
  margin: ${Grid['16px']};
  flex: 1;
  padding-bottom: ${Grid['32px']};
`;
