import { Colors } from '@kvika/theme';
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  background: {
    backgroundColor: Colors.black7,
    borderRadius: 16,
  },
  indicator: {
    backgroundColor: Colors.gold150,
    width: 72,
    height: 5,
    marginBottom: 8,
  },
});
